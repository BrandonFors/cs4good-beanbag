# Test Instuctions
make sure you're in the client directory

1. run `npm install`
2. run `npm run dev` - this will startup the frontend on your machine

Note that axios currently uses a version of the backend hosted on render.
If you want quicker response times you can host the backend on your local machine and change the axios endpoints to the machine hosted backend
